name: SynX
main: ${package}.SynXBukkit
version: ${project.version}
author: ${developer}

commands:
   synx:
      description: Manage SynX

permissions:
    synx.manage:
        description: Manage synx
        default: op
